# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: trading-research-bot
metadata:
  template: trading-research-bot@1.0.0

services:
  bot:
    project: src/TradingResearchBot
    language: dotnet
    host: function
